# Install Traefik
install:
    helm upgrade --install --namespace=traefik-v2 --create-namespace --dependency-update traefik .
    @echo "✅ Traefik Ingress installed successfully."

# Remove Traefik
remove:
    helm uninstall --namespace=traefik-v2 traefik
    @echo "✅ Traefik Ingress removed."
