# Install Traefik
install:
    helm upgrade --install --namespace=traefik --create-namespace --dependency-update traefik .
    @echo "✅ Traefik Ingress installed successfully."

# Remove Traefik
remove:
    helm uninstall --namespace=traefik traefik
    @echo "✅ Traefik Ingress removed."
